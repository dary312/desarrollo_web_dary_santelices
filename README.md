-Me hubiera gustado ver aunque sea un poco la parte de estadistica, aunque para ser honesto, y por el tiempo, ni lo abri
-Esta vez agregue el modal para confirmar la subida del aviso
-al agregar las validaciones de flask fui agregando los mensajes arriba, podria haber quedado mas estetico
-una cosa que me hubiera gustado revizar mejor es que cuando no se envia algun dato obligatorio, y no lo captura el required del html,
(por ejemplo, para los radio buttons, no supe como agregar el required), se vuelve a la misma pagina, pasandole los mensajes de error,
el problema es que al hacer esto hay que llenar todos los campos denuevo y resulta tedioso
